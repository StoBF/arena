# UI Modules

This folder is the canonical location for UI logic modules.

- `scenes/` contains layout-focused scene scripts.
- `components/` contains reusable UI components.
- `modules/` contains orchestration logic and adapters that should not depend on concrete scenes.

Existing bridge modules in feature folders remain for backward compatibility and can be migrated here incrementally.
